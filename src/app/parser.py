from dataclasses import dataclass
import sympy

@dataclass
class Parser:
    """Class that parses user equation input."""

    def __init__(self):
        self.temp = "classe inutile ?"

    def parse(equation: str):
        
        sympy.parsing.sympy_parser